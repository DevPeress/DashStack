generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Usuario {
  id    Int    @id @default(autoincrement())
  usuario String
  email String
  password String
  todo Json
}

model Team {
  id    Int    @id @default(autoincrement())
  nome String
  sobrenome String
  email String
  celular String
  position String
  genero String
  foto String
}